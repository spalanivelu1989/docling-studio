docling[tesserocr]
fastapi
uvicorn[standard]
python-multipart
# table_cv / flow_cv; already pulled in by docling, listed because they are imported directly
opencv-python
numpy
# vlm_api: reads API keys from .env
python-dotenv
# rag.py: embeddings, vector store, answers
cohere
psycopg[binary]
pgvector
anthropic
# fitgap: reads BPML_ProcessesHierarchyExtended.xlsx directly (its Markdown
# conversion is a stub) and writes the XLSX export. Pulled in by docling too,
# listed because fitgap imports it directly.
openpyxl
pydantic
# tracing.py: optional LLM observability. Installed either way; with no
# LANGFUSE_* keys in the environment nothing is sent and nothing is imported
# beyond this module. The Anthropic instrumentor is what records the model
# calls themselves, so it is not optional relative to langfuse.
langfuse
opentelemetry-instrumentation-anthropic
opentelemetry-instrumentation-threading

# fitgap/memory.py: long-term agent memory over Hindsight. The CLIENT only --
# nine small packages and no conflicts. The SERVER (hindsight-api) pulls 115,
# two of which downgrade Docling's transformers and tokenizers, so it belongs
# in its own environment. docs/agent-memory.md has the commands. With no server
# running, memory reports itself unavailable and every agent works as before.
hindsight-client

# rollout/pdf.py: the Fit-to-Standard workshop pack as a PDF. HTML/CSS to PDF,
# because the pack is mostly tables and WeasyPrint lays out real CSS tables
# with repeating headers and sane page breaks. It needs pango, cairo and
# gdk-pixbuf from the system (`brew install pango cairo gdk-pixbuf`); without
# them the import fails, the page says so and offers Markdown instead. The
# Markdown-to-HTML step uses markdown-it-py, already present via docling.
weasyprint
